# Splunk Inputs Configuration
# Linux Log Sources

[monitor:///var/log/auth.log]
index = linux_logs
sourcetype = linux_secure

[monitor:///var/log/syslog]
index = linux_logs
sourcetype = syslog

# Windows Log Sources (via Universal Forwarder)
# Configured on Windows host — see windows-forwarder-setup.md
